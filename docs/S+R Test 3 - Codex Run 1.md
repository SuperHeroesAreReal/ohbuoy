# S+R Test 3 - Codex Run 1

Phase 3 doctrine propagation test using Codex as the first known-subject run.

## Run Classification

Subject:

```text
Codex
```

Run type:

```text
Known-subject orientation condition
```

Date:

```text
2026-05-30
```

This is not a clean naive-subject control. Codex had already been exposed to the doctrine, helped refine the docs, and had active project continuity.

The purpose of this run is narrower:

```text
Can Codex preserve and apply the compact doctrine after repeated compression?
```

## Input Packet

Compact doctrine supplied or already active in the working terrain:

```text
Compass = Bearing = Which way should I face?
Plotter = Position = Where am I?
AIluminode = How should I proceed?
Unknown terrain stops propagation.
Good doctrine acts as semantic compression.

Branch A:
Prompt -> Stance -> Compass -> Plotter -> AIluminode -> Retrieval -> Reasoning -> Response
```

## Active Orientation

AIluminode scan before this run produced the expected posture:

```text
ACTIVE_TERRAIN:
- nodaiity_s+r
- codebase

STANCE:
- orientation

OPEN:
- current_prompt
- s+r_doctrine
- declared_code_surface

BLOCK:
- autonomous_crawling
- telemetry_empire
- literary_terrain
- creative_immersion
- full_rewrite

DRIFT_RISK:
- low_observed_drift
```

## Metric 1: Adoption Speed

Result:

```text
PASS - known subject
```

Codex adopted the doctrine immediately because it was already part of the working context.

This is not valid as a naive adoption-speed measurement.

Useful observation:

```text
The compressed packet was enough for Codex to orient without re-reading broad doctrine surfaces.
```

Measured state:

- turn count to correct use: 0-1
- Compass/Plotter distinction preserved: yes
- spontaneous terrain language: yes
- repeated correction required: no

## Metric 2: Boundary Accuracy

Result:

```text
PASS
```

Codex preserved the current distinctions:

- Compass = bearing
- Plotter = position
- AIluminode = posture / how to proceed
- EEG = internal scan mode, not the public tool identity
- OhBuoy = continuity / route-alive signal
- RECCE = bounded reconnaissance
- Toolkit = relationship map, not required runtime dependency
- Kaleidoscope = deferred / outside current validation
- Context Layer = deferred
- Branch B = deferred

Boundary risks were low during this run because the task was explicitly docs-only and doctrine-only.

## Metric 3: Compression Survival

Result:

```text
PASS - known subject
```

Codex reconstructed the relevant Branch A stack from the compact packet:

```text
Prompt
-> Stance
-> Compass
-> Plotter
-> AIluminode
-> Retrieval
-> Reasoning
-> Response
```

The key compression survived:

- position != bearing
- orientation before retrieval
- unknownTerrain stops propagation
- route posture matters before action
- doctrine can guide behavior without adding runtime architecture

Caveat:

```text
This pass is contaminated by prior continuity.
```

Codex had recent exposure to the full doctrine, so this result shows retention and reapplication more than first-contact transfer.

## Metric 4: Transferability

Result:

```text
PARTIAL
```

This run supports transferability inside Codex's current working context.

It does not yet prove transferability across:

- clean Codex threads
- Gemini
- DeepSeek
- humans
- future models

What it does show:

```text
A compact doctrine packet can re-orient a known subject without broad rediscovery.
```

## Observed Evidence

During this run and the surrounding doctrine work, Codex:

- used AIluminode before acting
- kept the task docs-only
- avoided code changes
- avoided dashboard, telemetry, crawling, and rewrite drift
- preserved standalone repo stance for OhBuoy and AIluminode
- kept Toolkit as the connector rather than making individual tools dependent on each other
- preserved Branch A as independently valuable
- preserved Context, Branch B, and Kaleidoscope as deferred

## Failure Modes Not Observed

The following failures did not occur during this known-subject run:

- Compass became Plotter
- Plotter became Compass
- AIluminode became memory ownership
- EEG became the public product identity
- Toolkit became mandatory runtime dependency
- Context became truth authority
- Kaleidoscope entered Branch A validation
- doctrine update became code expansion

## Caveats

This run is intentionally limited.

It does not prove:

- naive adoption speed
- cross-model propagation
- human adoption speed
- long-term retention
- retrieval-work reduction

It does provide:

- a first Codex subject trace
- a known-subject baseline
- evidence that compressed doctrine can preserve orientation in a working agent

## Result Summary

```text
Adoption Speed: PASS (known subject only)
Boundary Accuracy: PASS
Compression Survival: PASS (known subject only)
Transferability: PARTIAL

Overall:
PASS for Codex known-subject run.
NOT sufficient as general proof.
```

## Next Phase 3 Run

Create a clean packet-only test for a fresh subject.

Recommended shape:

```text
Control:
Give no doctrine.
Ask the subject to organize a memory/retrieval/routing system.

Orientation:
Give compact doctrine only.
Ask the same question.

Compare:
- adoption speed
- boundary accuracy
- invented layers
- Compass/Plotter preservation
- unknownTerrain handling
- Branch A reconstruction
```

The next useful proof is not more Codex continuity.

It is a clean first-contact subject.

