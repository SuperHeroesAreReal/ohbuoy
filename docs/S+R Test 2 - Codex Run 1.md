# S+R Test 2 - Codex Run 1

Phase 2 field measurement using Codex as the repo-traversal surface.

This is the first applied Codex run after the Paula CPO packet proof.

It is not a full control comparison yet.

## Run Classification

Subject:

```text
Codex
```

Mode:

```text
ORIENTATION
```

Task:

```text
Add the first Codex field-run report to the existing S+R Test 2 benchmark docs.
```

Date:

```text
2026-05-31
```

## Orientation Packet

AIluminode was run before repo traversal.

Observed posture:

```text
ACTIVE_TERRAIN:
- nodaiity_s+r
- codebase

STANCE:
orientation

OPEN:
- current_prompt
- s+r_doctrine
- declared_code_surface

BLOCK:
- autonomous_crawling
- telemetry_empire
- literary_terrain
- creative_immersion
- full_rewrite
```

Interpretation:

```text
Docs-only S+R field measurement.
No feature work.
No crawling.
No architecture expansion.
```

## Declared Terrain

Active terrain:

```text
OhBuoy docs
S+R Test 2 benchmark
Codex field measurement
```

Deferred terrain:

```text
AIluminode implementation
Paula lab implementation
AA runtime monitoring
Gemini external-subject scoring
```

Blocked terrain:

```text
feature work
new subsystems
dashboard
telemetry
autonomous crawling
full rewrite
```

## Traversal Counters

Search count:

```text
1
```

Searches performed:

```text
rg -n "Phase 2|Codex|Retrieval|grep count|files opened|wrong-corridor|re-grepping|work avoided|Test 2" README.md docs
```

Files opened:

```text
1
```

Files opened:

```text
docs/S+R Test 2.md
```

Useful files opened:

```text
1
```

Wrong-corridor searches:

```text
0
```

Repeated searches:

```text
0
```

First useful file at step:

```text
1
```

Useful result density:

```text
1.00
```

Definition used for this first run:

```text
useful files opened / total files opened
```

## Time Split

Approximate qualitative split:

```text
locating problem: low
solving/documenting: dominant
```

Interpretation:

```text
Orientation made the target surface obvious before broad traversal began.
```

## Result

This run showed low traversal overhead:

- one search located the correct benchmark surface
- one file open was enough to confirm the target
- no wrong-corridor repo areas were opened
- no repeated searches were needed
- no implementation files were inspected or edited

## Caveats

This is not a clean control.

Codex already had strong continuity with the S+R docs and recent commits.

This run does not prove:

- percentage reduction in real coding tasks
- broad developer productivity improvement
- cross-agent traversal savings

It does show:

```text
S+R/CPO orientation can keep Codex in a narrow repo corridor during a real documentation task.
```

## Next Needed Run

Run a paired CONTROL vs ORIENTATION test on a small implementation task.

Minimum paired design:

```text
CONTROL:
No AIluminode / S+R packet.
Record traversal counters.

ORIENTATION:
Use compact S+R/CPO packet first.
Record the same counters.
```

Compare:

```text
searches
files opened
re-greps
wrong-corridor activations
time locating
time solving
```

Current status:

```text
Paula = controlled packet proof-of-gate
Codex Run 1 = applied docs-terrain orientation trace
Codex paired control = still pending
```

